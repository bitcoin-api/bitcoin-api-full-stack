'use strict';

const {
    utils: {
        aws: {
            dino: {
                getExchangeDatabaseEntry
            } 
        }
    }
} = require( '@npm.m.stecky.efantis/common-exchange' );


module.exports = Object.freeze({

    getExchangeDatabaseEntry,
});
